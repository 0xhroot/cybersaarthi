<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug builds may talk to an in-house backend over plain HTTP (LAN/emulator). -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>