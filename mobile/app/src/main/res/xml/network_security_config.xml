<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Release: cleartext HTTP is rejected. Field deployments must use HTTPS. -->
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>